import styled from 'styled-components';

const Common = styled.i`
  font-size: inherit;
`;

export const search = styled(Common).attrs({ className: 'icon-search' })``;

export const initialCheckbox = styled(Common).attrs({ className: 'icon-checkbox-initial' })``;

export const disabledcheckbox = styled(Common).attrs({ className: 'icon-checkbox-disable' })``;

export const activeCheckbox = styled(Common).attrs({ className: 'icon-checkbox-active' })``;

export const onCheckCircle = styled(Common).attrs({ className: 'icon-check-on-circle' })``;

export const offCheckCircle = styled(Common).attrs({ className: 'icon-check-off-circle' })``;

export const plus = styled(Common).attrs({ className: 'icon-plus' })``;

export const cross = styled(Common).attrs({ className: 'icon-x-square' })``;

export const tag = styled(Common).attrs({ className: 'icon-tag' })``;

export const edit = styled(Common).attrs({ className: 'icon-edit' })``;

export const archive = styled(Common).attrs({ className: 'icon-arichive' })``;

export const milestone = styled(Common).attrs({ className: 'icon-milestone' })``;

export const paperclip = styled(Common).attrs({ className: 'icon-paperclip' })``;

export const smile = styled(Common).attrs({ className: 'icon-smile' })``;

export const trash = styled(Common).attrs({ className: 'icon-trash' })``;

export const calendar = styled(Common).attrs({ className: 'icon-calendar' })``;

export const refresh = styled(Common).attrs({ className: 'icon-refresh-ccw' })``;

export const alertCircle = styled(Common).attrs({ className: 'icon-alert-circle' })``;

export const dropdownArrow = styled(Common).attrs({ className: 'icon-dropdown-arrow' })``;
